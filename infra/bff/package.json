{
  "name": "@nutriassistant/bff",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-For-Frontend for NutrIAssistant. Proxies OpenFoodFacts, FatSecret, and Spoonacular so API secrets never ship in the mobile bundle.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=20"
  }
}
