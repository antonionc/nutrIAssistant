// Android / web fallback — the iOS platform file (LiquidGlassTabBar.ios.tsx)
// provides the native SwiftUI Liquid Glass implementation on iOS 26+.
// Metro automatically picks the correct file based on the platform suffix.
export { CustomTabBar as LiquidGlassTabBar } from './CustomTabBar'
